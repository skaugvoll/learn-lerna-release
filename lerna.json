{
  "npmClient": "npm",
  "packages": ["packages/*"],
  "version": "independent",
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", ".env"]
    },
    "version": {
      "allowBranch": ["main", "production", "ignore-package-template"],
      "message": "chore(release): release",
      "ignoreChanges": ["**/*.md", ".env"]
    }
  }
}
