name: Create Prod Request
on:
  push:
    tags:
      - "**"
  release:
    types:
      - published
      - unpublished
      - created
      - edited
      - deleted
      - prereleased
      - released

jobs:
  create-pr:
    runs-on: ubuntu-latest
    steps:
      - name: "test trigger"
        env:
          MSG: I' was triggered
        run: echo $MSG
      - name: "Print version Package A"
        id: package-version-a
        uses: martinbeentjes/npm-get-version-action@main
        with:
          path: pacakges/a/package.json
